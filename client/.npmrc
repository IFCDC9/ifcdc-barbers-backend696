# Render sets NODE_ENV=production during builds, which skips devDependencies by default.
# Belt-and-suspenders: vite is in dependencies, but keep this for any future dev-only build tools.
production=false
